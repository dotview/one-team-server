import Email from '../src/email'

describe('test sending email', () => {

    test('testing email', async () => {
       let email = new Email()
        //expect(email.sendEmail({to: 'fe@163.com', subject:'test email', content: 'test sending email'})).not.toBeNull()
    })


})

